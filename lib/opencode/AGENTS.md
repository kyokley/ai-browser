# Browser startup

Before doing any other work, every agent must launch Chromium:

```bash
./lib/opencode/skills/ai-browser/scripts/launch-browser.sh
```

Use the CDP endpoint at `http://127.0.0.1:9222` for browser automation.
